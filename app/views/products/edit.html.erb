<%= simple_form_for @product do |f| %>
  <%= f.input :title %>
  <%= f.input :desc %> 
 <%= f.input :photo, as: :file %>  <!-- [...] -->
   <%= f.button :submit, "Update", class: 'btn btn-primary'%>
<% end %>